TODO
----

 - Get some real numbers!

 - How to avoid copying the same jar each time we launch a MapReduce job?
   see: http://markmail.org/thread/sxnq4ini5fb3tivm

 - Add RDFS inference (a la RIOT infer command).

 - Refactor download.java so that the download part is separate from the reconstruction of indexes.
 
 - Does this work?
   hadoop --config ~/.whirr/hadoop jar target/hadoop-deploy/tdbloader3-eploy.jar cmd.tdbloader3 -D overrideOutput=true -D copyToLocal=false s3n://$AWS_ACCESS_KEY_ID:$AWS_SECRET_ACCESS_KEY@{bucketname1} s3n://$AWS_ACCESS_KEY_ID_LIVE:$AWS_SECRET_ACCESS_KEY_LIVE@{bucketname2}
